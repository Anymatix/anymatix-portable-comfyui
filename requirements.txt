# Anymatix Portable ComfyUI - Base Requirements v2.0
# 
# Minimal packages needed for bootstrap.py to work.
# ComfyUI and custom node requirements are installed at runtime via pip.
#
# The app's bootstrap.py handles:
# - ComfyUI requirements.txt installation
# - Custom node requirements.txt installation
# - GPU-specific PyTorch installation (CUDA/ROCm/MPS)

# Git operations for bootstrap.py
pygit2

# Common utilities that benefit from being pre-installed
pyyaml
aiofiles>=0.8.0
aiohttp>=3.11.8
psutil
tqdm
